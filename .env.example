# MySQL Database Configuration
# Copy this file to .env and update with your actual values

# MySQL Connection String
# Format: Server=hostname;Port=port;Database=database_name;Uid=username;Pwd=password;
MYSQL_CONNECTION_STRING=Server=localhost;Port=3306;Database=yopo_backend;Uid=root;Pwd=your_mysql_password;

# Alternative connection string formats:
# For remote MySQL server:
# MYSQL_CONNECTION_STRING=Server=your_server_ip;Port=3306;Database=yopo_backend;Uid=your_username;Pwd=your_password;

# For MySQL with SSL:
# MYSQL_CONNECTION_STRING=Server=localhost;Port=3306;Database=yopo_backend;Uid=root;Pwd=your_password;SslMode=Required;

# Instructions:
# 1. Copy this file to .env
# 2. Replace 'your_mysql_password' with your actual MySQL root password
# 3. Update server, port, database name, and credentials as needed
# 4. Ensure your MySQL server is running and accessible
